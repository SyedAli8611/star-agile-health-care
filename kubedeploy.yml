apiVersion: apps/v1
kind: Deployment
metadata:
  name: healthcare-deploy
  labels:
    app: healthcare-deploy-lbl
spec:
  replicas: 3
  selector:
    matchLabels:
      app: healthcare
  template:
    metadata:
      labels:
        app: healthcare
    spec:
      containers:
      - name: healthcare-container
        image: iamfaizanali/syed_healthcare
        ports:
        - containerPort: 8082
---
apiVersion: v1
kind: Service
metadata:
  name: healthcare-np-service
  labels:
    app: healthcare
spec:
  selector:
    app: healthcare-deploy-lbl

  type: NodePort
  ports:
  - nodePort: 30252
    port: 8082
    targetPort: 8082
